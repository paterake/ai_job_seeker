"""ai_job_seeker — profile -> match -> draft -> packet.

Deterministic plumbing with a single swappable LLM-authoring step. Phase
packages are added as the pipeline is built; Stage 1 ships `profile` only.
"""

__version__ = "0.1.0"
